extends layout

block content
    h1= title


    ul
        each poop in poop_list
            li
              a(href=poop.url) #{poop.description}
              |    (#{poop.user})

        else
            li There are no poops.